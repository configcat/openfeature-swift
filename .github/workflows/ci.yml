name: CI

on:
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '**.md'
  push:
    branches: [ '*' ]
    paths-ignore:
      - '**.md'

jobs:
  Tests:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v5
    - run: swift test